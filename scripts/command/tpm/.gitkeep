// ✦━━━━━━━━━━━━━━━━━━━━━✦
// 🗂️ Meheraz Bot - TPM Folder Keeper
// ✦━━━━━━━━━━━━━━━━━━━━━✦

const fs = require("fs");

const dir = "./tpm";
const file = `${dir}/.gitkeep`;

fs.mkdirSync(dir, { recursive: true });
fs.writeFileSync(file, "# Meheraz TPM Directory - Do Not Delete\n");

console.log("✅ tpm/.gitkeep file created successfully!");
